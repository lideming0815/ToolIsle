# ToolIsle · 项目分组与标签筛选（gitee.5）

基于已验证的 gitee.4 / 1e7ea32073f4513f18dccccb3c2ef7aac6b776bc。版本 2.3.3（1643）。

## 本次变化

阅读窗口中的状态控件只占一行，优先呈现「未完成、进行中、已关闭」。剩余状态在有空间时继续平铺，否则收入 `…` 菜单；菜单中的所有状态仍可选择，选中隐藏状态时标题行显示当前值，避免隐藏条件。默认状态仍为未完成。

Issues 按设置中已选择的项目分组；组头显示项目名与当前已加载匹配数，可折叠。组内不重复显示项目名和编号。折叠与筛选是不同操作，折叠不会影响候选标签统计、已选项目或右侧阅读。真实账户的折叠状态按账户与仓库 ID 在本机保存；退出账户清除该账户的折叠记录。原项目筛选已停止参与结果计算，没有隐藏的旧项目过滤条件。

列表行以标题为主，辅以状态和标签的彩色 Tag、更新时间。Tag 区最多一行，溢出 `+N` 只展开该 Issue 的标签，不自动修改筛选。列表中没有复制图标；保留右键复制。顶部工具顺序为阅读选项（字号）→复制链接→刷新→网页打开→设置。刘海没有复制按钮，也没有新增标签筛选表单。

标签候选来自所有已选项目的已加载 Issues，再按当前状态、关键词限定；不按标签筛选结果或折叠状态反推。两行平铺，更多通过 `… +N` 弹出可搜索、多选的面板。多选匹配任意一个标签；清除标签不影响状态和搜索。已选而不在新范围内的标签保留在「已选 N 项」中，可取消。标签数量是当前候选数据范围内的 Issue 数量，而非整个仓库总量。

同名（区分大小写）标签跨项目聚合；颜色冲突使用中性色。缺失标签字段不当成明确无标签，界面会提示部分记录未提供标签。不批量额外读取每个 Issue，也不修改任何 Gitee 数据。需要服务器返回可用的 labels 数组，真实私有项目返回结构仍需本机验证。

## 验证与安装

本包在 Apple Silicon/macOS CI 上使用原 Xcode Target Release 构建；结果以随包日志为准。包含只在显式演示参数下运行的合成数据探针，不使用用户令牌、不访问私有项目。真实 Gitee 权限、实体多屏和第三方 AltTab 尚未由合成数据覆盖。

临时签名、未经 Apple 公证；声明最低 macOS 14.6。仍保留原 Bundle ID、配置域、扩展协议及更新器。先退出旧包及官方 Atoll，备份旧应用，再替换 ToolIsle.app。无需重置账户或项目；验证期间不要安装上游应用内更新。打开阅读窗口后可以在状态的 `…` 中选择「全部」，查看所有已加载状态。

本轮不重写窗口生命周期、刘海尺寸规则、媒体、锁屏、Shelf、计时器、关联跳转或 Markdown 渲染；许可证、版权和原 Logo 保持不变。
