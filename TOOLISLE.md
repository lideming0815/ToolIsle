# ToolIsle — Atoll 原版基线验证版

本分支撤销独立工具箱的运行入口，直接以改造前 Atoll 提交 `83d15438efc5254635c5b425406d6603c80b8ece` 为基线。保留 `DynamicIsland.xcodeproj` / `DynamicIsland` scheme、全部原版功能、源码、配置、默认值和锁定依赖。

本轮只替换维护者提供的 Logo（各渠道 AppIcon、原有 logo/logo2 图像集、README 图标），更新文档并增加独立打包验证工作流。不改任何应用 Swift/Objective-C/C++ 代码、Info.plist、entitlements 或 project.pbxproj；不添加新工具箱、Gitee 或 Markdown 阅读器。原始 LICENSE、NOTICE、COPYRIGHT_ASSETS、版权文件头不变。

## 本轮验证方式

- 先核对源文件与固定基线逐字节一致，除 Logo、文档和预览构建工作流外的任何差异必须失败。
- 使用原版 Xcode 工程构建；不使用独立 Swift Package 应用入口，不传入 `--uitesting` 隐藏原版能力。
- 记录 macOS/Xcode/架构、构建、签名与启动检查结果。预览包不是 Developer ID 公证发布；无维护者证书时只能提供临时签名，不能宣称等价于官方签名版本。
- CI 不能替代实体 Mac 上的刘海、多屏、媒体、锁屏、TCC 权限、相机、扩展等交互验收。

## 运行前注意

应用名仍为 `Atoll.app`，Bundle ID、数据目录、端口和原始更新器也保持 Atoll 行为。这是原版功能对照包，不是可与 Atoll 同时运行的独立身份版本。

先退出旧 ToolIsle 与已运行的 Atoll；备份原应用，不要直接覆盖唯一副本，不要同时运行两个 Atoll。不会主动删除或迁移任何用户配置。可使用单独 macOS 测试账户避免旧偏好干扰。本轮不要安装应用内更新，否则会换回上游官方包并丢失替换 Logo。

预览包未经公证；不要全局关闭 Gatekeeper 或 SIP。系统权限请按需要正常授权。若签名造成启动或能力限制，须在交付说明中明确列出，不能靠关闭原版功能让检查通过。

## 手工回归清单

1. 启动/欢迎页、刘海展开收起、原标签、设置、快捷键、多屏。
2. 音乐控制与波形、系统状态、计时器、剪贴板、Shelf 拖放。
3. 日历、相机、屏幕录制、锁屏组件和扩展（按需授权与启用）。
4. 正常退出、重新启动，检查服务退出与设置保存。

## 维护方向

未来继续以 Atoll 为主体，保持原目录与类型名称。每次新能力独立提交，尽量新增模块和小入口，不改写整个 App 生命周期，不默认关闭原有功能。先确保原版回归，再逐项增加团队工具。

本验证分支由原版提交创建，现有 `dev` 和独立工具箱历史保留。切换到本分支可直接验证原版；不要把普通合并误认为对独立工具箱的反向回滚，正式回滚应另以明确的撤销提交完成。
