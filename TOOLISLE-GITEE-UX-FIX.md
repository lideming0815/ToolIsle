# ToolIsle Gitee UX 修复 · gitee.4

基于 gitee.3 / 60bf67dfa3109c90257c1cdd780a56e3b26e8e07；版本 2.3.3 (1642)。

## 功能

Gitee 独立设置新增「刘海预览 → 最多显示条数」，默认 8，允许 1–10。按照筛选后的已加载内容计算高度，屏幕空间不足时中间列表滚动。标题、操作和底部阅读入口不随列表滚出。高度计算同时用于 Gitee 内容、实际 NSWindow 和鼠标范围，并按刘海所在显示器限高，不以阅读窗口的焦点屏幕决定。刘海没有复制按钮。

阅读窗口的筛选区域提供搜索清空、明确的项目/状态标签、匹配/已加载数量及清除筛选。只筛选已加载内容，不修改 Gitee Watch/Star 或查看项目，不切换正在阅读的 Issue。刘海的「调整筛选」打开并定位阅读窗口筛选区。

阅读窗口详情工具栏与列表提供复制 Issue 网页链接，右键入口保留。按钮写入成功后暂时显示勾号；失败有提示。详情可用 Shift-Cmd-C，不覆盖普通文字的 Cmd-C。只复制网页 URL，不附带令牌；刘海不增加复制操作。

## 窗口修复

旧设置窗口关闭时无条件 setActivationPolicy(.accessory)，阅读窗口本身也没有拥有普通应用的激活生命周期。新版让已打开（含隐藏或最小化）的阅读窗口持有 regular 状态；设置关闭后，在应用仍活跃且阅读窗口未关闭/最小化时恢复阅读焦点。设置窗口独立使用时不创建阅读窗口；阅读窗口最后关闭后回到后台模式。原刘海仍为 Atoll 非激活面板，不变成普通窗口。

阅读窗口打开期间 ToolIsle 会显示在 Dock/系统应用切换器中，这是使用系统切换能力的预期行为；只保留刘海时不永久常驻 Dock。阅读窗口使用普通 NSWindow、AXStandardWindow 子角色，不在失活时隐藏。macOS 系统切换为 Cmd-Tab；第三方 AltTab 的 Option-Tab 仍受该工具的权限、过滤和 Space 设置影响。

## 验证边界

发布前运行原 Xcode Release 构建、既有回归、494 条生产高度规则断言，以及显式合成数据的真实窗口/刘海/剪贴板探针。结果以随包报告为准，不以本说明代替实际日志。真实私有 Gitee 权限、网络、实体双屏/合盖和第三方 AltTab 热键未由合成测试覆盖。无用户令牌或私有内容进入公共 CI。

本测试包适用于 Apple Silicon，最低系统声明 macOS 14.6；临时签名、未经公证，保留上游 Bundle ID/配置域/扩展协议/更新器。安装前退出旧包和官方 Atoll 并备份应用，不同时运行，不在验证期间接受上游应用内更新。不需要清空设置、重新选择项目或重新发送令牌。

许可证、版权、Logo 与原 Atoll 业务功能保持原有配置；不实现本地 Markdown 文件读取或新的通用工具箱。
