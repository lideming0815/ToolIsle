## Agent skills

### Issue tracker
使用 GitHub Issues，见 `docs/agents/issue-tracker.md`。

### Triage labels
使用默认五种分流标签，见 `docs/agents/triage-labels.md`。

### Domain docs
采用 single-context 结构，见 `docs/agents/domain.md`。
