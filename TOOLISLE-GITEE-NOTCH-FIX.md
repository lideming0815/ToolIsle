# ToolIsle gitee.3 · 刘海内容可见性修复

基于 gitee.2 / f8f0b6df395394d69d066ede0da58eeedaef06d9。版本 2.3.3，构建号 1641。

## 修复范围
仅修改 Gitee 刘海摘要视图，保留原独立 Gitee 设置、API/令牌、关联 Issue 阅读器、媒体、锁屏、Shelf、选屏与原窗口管理。

刘海背景固定黑色，旧摘要使用跟随系统的文字颜色，浅色模式存在黑字落在黑底上的风险。新视图在自身子树限定深色文字环境，显式使用白色/灰白色，不改变整个应用或设置窗口主题。内容高度在已有 250pt 刘海预算内固定为 190pt；超长内容在摘要内部滚动，不放大刘海。

旧逻辑仅检查未筛选 items 是否为空，却渲染 filteredItems；有数据但被搜索/状态/项目过滤时没有空状态。新版显示“当前筛选没有匹配的 Issue”及清除筛选按钮。清除筛选仅修改新阅读模块的筛选，不清除账户、项目选择或 Atoll 设置。

标题、独立设置和阅读窗口按钮始终有可见入口。连接中/连接失败/未连接、未选择项目、列表加载中/失败/空结果/无匹配、正常和部分成功均有明确内容，不把网络失败伪装成空列表。读取失败的真实 HTTP 状态仍在独立 Gitee 设置中查看。

## 使用
退出旧包及官方 Atoll，备份后使用 ToolIsle.app。打开刘海的 Gitee 标签。若提示筛选无匹配，点击“清除筛选，显示已加载 Issues”。若提示读取失败，进入齿轮所指向的独立 Gitee 设置查看状态、重试；无需清空所有设置。

这是临时签名、未经 Apple 公证的 Apple Silicon 测试包。最低系统声明 14.6，实际测试版本以 environment.txt 为准。仍保留上游 Bundle ID 与更新器，不同时运行官方 Atoll/旧验证包；测试期间不接受上游应用内更新。

## 验证边界
本次工作流将编译真实原 Xcode Target，并对实际 GINotchView 做浅色/深色、多个状态和宽度的原生 SwiftUI 渲染检查；另直接打开原应用刘海页面截图，而不是只截图阅读窗口。数据明确是合成测试数据，不是用户私有项目。

最终是否通过以工作流实际结果及报告为准。本次不使用用户此前发送的令牌、不把私有项目或凭据送入 CI。渲染修复不能证明真实账户 API 访问成功；如状态提示授权或请求失败，需结合本机诊断继续验证。

正式源码在独立修复分支，通过 PR 审查，不自动合并或覆盖 dev。原 GPL、版权与第三方许可证保留。
