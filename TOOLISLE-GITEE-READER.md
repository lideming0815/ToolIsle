# ToolIsle · Gitee Issue 阅读验证版

## 目的与基线

基于 `9527eed3a9dcf64edbea65374de6c643265b68d3`（Atoll 原工程 + Logo/名称 + 内置刘海屏优先修复）局部添加 Gitee 阅读能力。不恢复独立 Swift Package 工具箱，不迁移媒体、计时器、锁屏、剪贴板、Shelf 或显示器管理。

应用仍使用 `DynamicIsland.xcodeproj` / `DynamicIsland` scheme，内部模块名称保留 Atoll。应用显示名 ToolIsle，版本 2.3.3，构建号 1639。

## 使用

1. 在菜单栏打开 `Gitee Issues…`，或在设置 → General 中启用 Gitee 阅读。默认关闭。
2. 输入个人访问令牌，验证身份后通过右上角账户按钮选择 Watch / Star 来源的项目。勾选后点“完成”。不会修改平台上的 Watch/Star。
3. 在原标准模式标签栏中进入 Gitee，或直接使用菜单栏入口。极简模式不改变原标签机制。
4. 阅读窗口中左侧筛选已加载的标题、编号、项目和状态；右侧显示正文和评论。
5. 点击同仓库、跨仓库或未加入查看列表的 Issue 链接，在同一个窗口打开；后退/前进恢复阅读历史，左侧筛选不随跨仓库跳转改变。
6. 本地文件、仓库 Markdown 文件浏览、Issue 编辑、评论提交均不在本期范围。非 Issue 的普通链接由默认浏览器打开。

## 关联链接与定位

支持 `https://gitee.com/{owner}/{repo}/issues/{number}`、完整或相对 Markdown 链接、正文与评论里的链接。Issue 编号按字符串保留大小写。

支持已识别的 `note_<id>`、`issuecomment-<id>`、`comment-<id>`、`comment_<id>` 评论锚点及生成的标题锚点。定位评论时按页读取，最多加载 1000 条；未知或未找到的定位会提示，并保留原链接供网页打开。不保证失效或无权访问的 Issue 可被读取。

代码块不会被当作链接扫描。`javascript:` / `file:` / 危险 URL、含凭据链接不会执行。链接只由统一路由处理，不允许嵌入网页自行跳转。

## 数据与安全

- 仅发送读取 API 请求。个人令牌的实际权限仍由 Gitee 授权决定，不意味着令牌本身只读。
- 令牌通过原生网络层发送至固定的 `https://gitee.com/api/v5`，只放 Authorization header；拒绝 API 重定向，避免泄漏给其他站点。
- 使用独立 Keychain 服务 `io.github.lideming0815.toolisle.gitee-reader`。不写入 URL、HTML、源代码、日志或 UserDefaults。
- 内存缓存最多 30 个详情页，导航历史最多 100 步。退出账户/关闭模块清除正文和评论。项目选择元数据按账户 ID 单独保存，不持久化正文。
- 请求按需触发，不引入定时后台轮询；多个显示器共用同一数据服务。
- WKWebView 为非持久数据存储，只显示本地生成的已清洗 HTML。Marked 和 DOMPurify 打包在应用内，运行时不加载 CDN 脚本。
- 远程图片默认关闭。用户允许后仅内嵌 Gitee 及其指定图片域的 HTTPS 图片；不会把令牌发给图片地址。其他图片提供浏览器入口。需要网页登录的图片可能无法内嵌。
- 不把私有内容发送到 AI，不增加锁屏 Issue 提示，不读取本地文件或剪贴板；复制操作必须由用户触发。

## UI 和原版兼容

原默认首页不变。团队功能关闭时不包装原标签布局；开启后允许标签横向滚动，避免挤压原按钮。阅读窗口与刘海位置独立，移动阅读窗口不修改选屏偏好。

两栏阅读器沿用原生 macOS 控件，支持正文选取、代码复制、表格/代码局部横向滚动、字体大小调整和系统深浅色。正文与评论为连续滚动，不叠加多层正文滚动区。

## 验证范围

`tests/GiteeReaderRegression.swift` 直接编译生产的路由、历史和 API 实现，使用本地 URLProtocol 假响应测试，不是真实 Gitee 账户联调。

`--gitee-reader-demo` 可显示明确标记的合成演示数据，不读写保存的令牌，不访问 Gitee。`--gitee-reader-smoke` 仅用于 CI：A → B 评论 → 后退 A，导出实际 WKWebView 的检查结果；不使用上游 `--uitesting` 模式。

真实账户的 Watch/Star 分页、私有项目权限、真实评论锚点与图片、中文输入、键盘导航、外接屏/合盖/锁屏、播放控制和原扩展仍应由维护者在实体 Mac 验收。进程启动通过不等于完整原版功能已实测。

## 测试包注意

测试包临时签名，未 Apple 公证。仍保留上游 Bundle ID、XPC/RPC、配置域及更新器，未把发行身份迁移混入本次功能。先备份原应用和重要配置，退出官方 Atoll/旧测试包，不同时运行；验证期间不要安装上游应用内更新。正式团队分发前，应另行处理独立签名、身份与更新源。

## 上游同步与许可

旧文件只允许局部接入：页面枚举、标签、主视图分支、协调器的功能关闭回退、菜单/演示参数、原设置的一节和构建号。媒体控制、权限、锁屏、显示器选择、默认常量与依赖锁文件不改。

保持原 LICENSE、NOTICE、版权与第三方声明。新增代码遵循 GPL-3.0-or-later。Markdown 组件许可与确切版本在 `third_party/gitee-markdown` 及应用资源 `gitee-markdown-licenses.txt` 中记录。
